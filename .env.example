# Copy to .env for local development (never commit .env)
# Backend
PORT=3001

# Database (Prisma). Path is relative to src/backend/prisma/
# npm run db:* syncs this file to src/backend/.env for the Prisma CLI
DATABASE_URL="file:../database/dev.db"
